# Vue组件在线编辑与调试工具项目 Agent 执行计划

# 一、计划总览

核心目标：开发一个Vue3+Vite架构的网页工具，实现「左侧代码编辑+样式调试 + 右侧实时渲染」的核心功能，支持代码复制、组件打包下载（含使用文档）、PNG效果截图下载，聚焦简单小组件（如程序员简介卡片）开发，满足用户“可视化编辑+快速导出”的需求。

执行原则：分步落地、模块拆分、可验收、可复用，每个阶段完成后进行校验，确保Agent执行无偏差，最终交付可直接运行的完整项目。

技术栈：Vue3 + Vite + monaco-editor（代码编辑）+ html-to-image（PNG截图）+ prettier（代码格式化）

# 二、执行阶段拆解（分6个阶段，按顺序执行，每个阶段有明确产出）

## 阶段1：环境初始化（Agent执行优先级：最高，耗时预估：15分钟）

### 阶段目标

完成Vue项目初始化，配置核心依赖和环境，确保项目能正常启动，为后续开发奠定基础。

### Agent执行任务

1. 分析需求，选择合适的依赖并安装

2. 分析需求确定项目结构，创建基本目录（src/components、src/utils）

### 工具/命令

终端、npm、Vite、monaco-editor插件

### 验收标准

1. 项目能正常启动，无依赖报错；2. vite.config.js配置正确，包含monaco-editor插件和路径别名；3. 核心依赖全部安装完成（node_modules目录下有对应依赖文件夹）。

## 阶段2：项目目录与主布局搭建（Agent执行优先级：高，耗时预估：20分钟）

### 阶段目标

规划合理的项目目录结构，完成主页面（左右分栏）布局，实现左侧（代码编辑+样式调试）和右侧（实时渲染）的基础布局，确定页面样式规范。

### Agent执行任务

1. 按规划创建目录结构：src/components（存放核心组件）、src/utils（存放工具函数）

2. 编辑App.vue，实现左右分栏布局（左侧50%宽度，右侧50%宽度），设置容器样式（全屏显示、溢出隐藏）

3. 在App.vue中引入预留组件（CodeEditor、StylePanel、Preview、Toolbar），定义核心响应式数据（代码内容code、样式配置customStyle、刷新标识renderKey）

4. 设置基础样式（左侧深色背景、右侧浅色背景，布局居中对齐），确保布局适配不同屏幕尺寸

### 工具/文件

Vue单文件组件（.vue）、CSS样式、VS Code（编辑文件）

### 验收标准

1. 目录结构完整，符合规划；2. 启动项目后，页面呈现左右分栏布局，无样式错乱；3. 响应式数据定义正确，无语法错误。

## 阶段3：核心组件开发（一）—— 代码编辑器与实时预览（Agent执行优先级：高，耗时预估：30分钟）

### 阶段目标

开发左侧CodeEditor组件（VSCode同款编辑器）和右侧Preview组件，实现“左侧写代码、右侧实时渲染”的核心功能，支持Vue语法高亮和代码实时更新。

各个模块（代码、样式面板、预览页等各个组件），其位置都可以拖拽磁吸到页面的任意位置，用户可以根据自己的习惯调整布局，甚至可以将某个模块单独拖拽出来成为一个独立的窗口，支持多窗口操作。

### Agent执行任务

1. 开发CodeEditor.vue组件：引入monaco-editor，配置Vue语法高亮、代码提示，实现代码双向绑定（与App.vue中的code数据联动），设置编辑器高度、字体大小等基础样式。

2. 开发Preview.vue组件：通过Vue动态编译（createApp、compile），将用户编写的代码渲染为实际组件，实现代码修改后右侧实时刷新（通过renderKey触发重新渲染）。

3. 测试联动效果：在CodeEditor中编写简单的Vue组件代码（如程序员简介卡片），验证右侧Preview能实时渲染，无语法报错。

4. 优化编辑器体验：添加代码格式化功能（集成prettier），实现一键格式化代码。

### 工具/依赖

monaco-editor、prettier、Vue动态编译API

### 验收标准

1. 代码编辑器正常显示，支持Vue语法高亮、智能提示；2. 代码修改后，右侧预览实时更新，渲染效果正确；3. 代码格式化功能可用，格式规范。

## 阶段4：核心组件开发（二）—— 样式调试面板（Agent执行优先级：中，耗时预估：25分钟）

### 阶段目标

开发StylePanel组件，实现样式可视化调试功能（调色盘、圆角滑块），确保调试操作能同步更新左侧代码和右侧预览，实现“可视化调试+代码同步”。

### Agent执行任务

1. 开发StylePanel.vue组件：添加样式调试控件，包括颜色选择器（控制背景色、字体色）、滑块（控制圆角、内边距、阴影），绑定customStyle响应式数据。

2. 实现样式与代码同步：当用户通过控件调整样式时，自动修改CodeEditor中的对应CSS代码（如调整圆角，同步修改code中的border-radius属性）。

3. 优化调试体验：实时显示当前样式参数（如圆角数值、颜色值），支持重置样式功能，确保调试操作流畅无卡顿。

4. 测试同步效果：调整调色盘和圆角滑块，验证右侧预览样式同步变化，左侧代码中的CSS对应修改，无同步延迟。

### 工具/控件

Vue响应式API、原生颜色选择器、HTML滑块控件

### 验收标准

1. 样式调试面板控件齐全，操作正常；2. 调试操作同步更新预览和代码，无偏差；3. 支持样式重置，参数显示准确。

## 阶段5：核心组件开发（三）—— 工具栏与导出功能（Agent执行优先级：中，耗时预估：25分钟）

### 阶段目标

开发Toolbar组件，实现代码复制、PNG截图下载、组件打包下载（含使用文档）三大功能，满足用户“快速导出、开箱即用”的需求。

### Agent执行任务

1. 开发Toolbar.vue组件：添加三个功能按钮（复制代码、下载PNG、打包组件），设计简洁的按钮样式，绑定点击事件。

2. 实现代码复制功能：集成浏览器剪贴板API，点击按钮复制CodeEditor中的代码，复制成功后给出提示。

3. 实现PNG截图下载：使用html-to-image库，对右侧Preview区域进行截图，生成PNG文件，通过file-saver下载，确保截图清晰、无失真。

4. 实现组件打包下载：使用jszip库，将用户编写的代码、自动生成的props/method使用文档（TXT格式）打包为ZIP文件，支持一键下载，确保打包文件可直接解压使用。

5. 测试导出功能：分别测试复制代码、下载PNG、打包组件，验证功能正常，导出文件可用（PNG清晰、ZIP解压后有代码和文档）。

### 工具/依赖

html-to-image、jszip、file-saver、浏览器剪贴板API

### 验收标准

1. 工具栏按钮功能正常，点击有响应；2. 代码复制成功，可粘贴使用；3. PNG截图清晰，下载后可正常打开；4. ZIP包包含代码和使用文档，解压后可直接使用。

## 阶段6：优化、测试与交付（Agent执行优先级：中，耗时预估：20分钟）

### 阶段目标

优化项目体验，修复潜在bug，完成全面测试，确保项目稳定运行，交付可直接部署的完整项目。

### Agent执行任务

1. 体验优化：调整组件布局间距、字体样式，优化编辑器滚动体验，添加操作提示（如复制成功、下载中），提升用户体验。

2. bug修复：排查代码同步延迟、截图失真、打包失败等潜在问题，确保所有功能正常运行，无报错。

3. 全面测试：测试不同场景（编写复杂一点的组件、频繁调试样式、多次导出），验证项目稳定性；测试不同浏览器（Chrome、Edge），确保兼容性。

4. 交付准备：整理项目说明文档（启动命令、功能介绍、依赖说明），确保项目可直接启动、部署，无需额外修改。

### 工具/方法

浏览器开发者工具、多浏览器测试、bug排查工具

### 验收标准

1. 项目无bug，所有功能正常运行；2. 操作流畅，无卡顿、同步延迟；3. 兼容主流浏览器；4. 交付包含完整项目代码、说明文档，可直接启动部署。

# 三、Agent执行约束与注意事项

1. 执行顺序：严格按照阶段1-阶段6的顺序执行，前一阶段验收通过后，再进入下一阶段，避免流程混乱。

2. 依赖安装：若安装依赖时出现报错，优先排查npm版本、网络问题，确保所有核心依赖安装成功后，再进行后续开发。

3. 代码规范：所有组件代码遵循Vue规范，命名统一（如组件首字母大写），注释清晰，便于后续维护。

4. 异常处理：若遇到组件渲染失败、导出功能异常等问题，先排查代码语法错误、依赖配置问题，再逐步调试。

# 四、交付物清单（Agent执行完成后需提交）

1. 完整的Vue项目代码（含所有组件、工具函数、配置文件）；

2. 项目说明文档（启动命令、功能介绍、依赖说明、使用方法）；

3. 测试报告（验证所有功能正常运行，无bug）。
> （注：文档部分内容可能由 AI 生成）